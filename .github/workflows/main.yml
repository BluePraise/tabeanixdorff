name: Nightschool CI

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:
  push:
    branches: [develop]
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ftp.nightschool.studio
        username: ftp@nightschool.studio
        password: ${{ secrets.FTPPASSWORD }}
        server-dir: ./www/libra.nightschool.studio/public_html/tn/wp-content/themes/tabeanixdorff/
        exclude: |
          **/.git*
          **/.git*/**
          **/.github/**
          .DS_Store
